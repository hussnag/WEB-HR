import { PAddRowDirective } from './p-add-row.directive';

describe('PAddRowDirective', () => {
  it('should create an instance', () => {
    const directive = new PAddRowDirective();
    expect(directive).toBeTruthy();
  });
});
